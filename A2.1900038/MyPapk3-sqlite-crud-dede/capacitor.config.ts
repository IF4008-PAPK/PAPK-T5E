import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MyPapk3-sqlite-crud-dede',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
