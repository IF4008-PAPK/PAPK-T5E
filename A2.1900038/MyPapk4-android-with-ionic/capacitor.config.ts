import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MyPapk4-android-with-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
